apiVersion: v1
kind: Service
metadata:
  annotations:
  name: hypercloud-workflow-webhook-svc
  namespace: argo
  selfLink: /api/v1/namespaces/argo/services/hypercloud-workflow-webhook-svc
spec:
  ports:
  - port: 443
    targetPort: webhook
  selector:
    hypercloud: workflow-webhook
  sessionAffinity: None
  type: ClusterIP
